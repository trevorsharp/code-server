{
  "$comment": "Config for the workflow plugin (~/.config/opencode/plugins/workflow.ts). A project can override any key via <worktree>/.opencode/workflow.json. Restart opencode after editing because model profiles are baked into the tool description at startup. 'models' is the allowlist of exact model and reasoning-variant pairs for workflow agents; each 'note' guides when to select that profile and name distinct verifier perspectives. Empty list means agents use the session default model.",
  "enabled": true,
  "models": [
    {
      "slug": "openai/gpt-5.6-sol",
      "variant": "medium",
      "note": "The jack of all trades. Use for most general tasks, investigation, synthesis, and moderately complex work."
    },
    {
      "slug": "openai/gpt-5.6-sol",
      "variant": "high",
      "note": "The implementer. Use for complex implementation, and larger-scale changes."
    },
    {
      "slug": "claude-cli/fable",
      "variant": "high",
      "note": "The strategic planner. Use for architecture, design, strategic tradeoffs, and long-horizon planning through Claude Enterprise. Cannot make changes directly and doesn't have access to MCPs or additional tools."
    },
    {
      "slug": "claude-cli/fable",
      "variant": "low",
      "note": "The critic. Use for code review, consistency checks, scope reduction, and bounded adversarial review through Claude Enterprise. Cannot make changes directly and doesn't have access to MCPs or additional tools."
    },
    {
      "slug": "openai/gpt-5.6-sol",
      "variant": "low",
      "note": "The runner. Use for deterministic lookup, extraction, classification, formatting, scans, and most browser use. Escalate when work is ambiguous or judgment-heavy."
    }
  ],
  "maxConcurrency": 8,
  "maxAgentsPerRun": 100,
  "agentTimeoutMs": 1800000
}